# Python
venv/
__pycache__/
*.pyc

# Build C++ / CMake
build/

# Banco vetorial Chroma (regenerável pelo indexer)
banco_chroma/

# NeoPZ agora é um git submodule (base_de_dados/neopz) pinado na revisão
# validada — clone com: git clone --recursive

# Log de interações (dataset local — cresce a cada conversa)
logs/

# Build das soluções de referência (regenerável via CMake)
reference_solutions/build/

# Sistema / editor
.DS_Store
.vscode/